// Should display the book results from a search
// including title, image and author
// mock an API call to show this
// it should move to the next page on click of pagination buttons

describe('bookResults', () => {
    it.skip('should display the necessary book results from a search', () => {
      // mock api call
      // mock output
    });
    it.skip('should move to the next page of pagination when button is clicked', () => {
      // render book results 
      // render pagination component
      // mock clicks
    });
  });
  